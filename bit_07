//Q7)A program to switch off the  5th bit
#include <stdio.h>

main()
{
    int a,bit,mask=0x20;
    printf("Enter an integer: ");
    scanf("%d",&a);
    printf("a = %d\t",a);
    a=a&~mask; 
    printf("After swtching off the  5th bit, the value  of  is:\n");
    printf("a = %d\t",a);
}
